import { ActivarcontasenaPipe } from './activarcontasena.pipe';

describe('ActivarcontasenaPipe', () => {
  it('create an instance', () => {
    const pipe = new ActivarcontasenaPipe();
    expect(pipe).toBeTruthy();
  });
});
